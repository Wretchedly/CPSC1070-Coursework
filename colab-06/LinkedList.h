#ifndef LINKEDLIST_H
#define LINKEDLIST_H

#include <stdio.h>
#include <stdbool.h>

#include "Node.h"

void insert(char[]);
void deleteList();
bool isEmpty();

void print();

void printList(node*);
node* makeList(void);

#endif
